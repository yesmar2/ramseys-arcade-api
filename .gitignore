node_modules/
dist/
data/*.json
!data/.gitkeep
.env
# Any local env variant — .env.production.bak and friends carry real
# credentials, and only the template belongs in the repo.
.env.*
!.env.example
*.log
.DS_Store
.neon
# Rows lifted out of a live table by scripts/prune-impossible-records.mjs —
# the only copy of what was deleted, and player data besides. Keep it local.
pruned-records-*.json
# Whole-database copies written by `npm run seed:world -- --fresh` before it
# wipes anything. Every table, player data included. Keep it local.
backups/
# Throwaway sessions the load test makes on its own database (scripts/loadtest).
scripts/loadtest/accounts.json
# CPU profiles and their triggers (scripts/loadtest/profile-hook.mjs).
scripts/loadtest/*.cpuprofile
scripts/loadtest/profile.start
scripts/loadtest/profile.stop
